const { BlogPost } = require('../models');

const validateUpdateUser = async (id, tokenId) => {
  const { userId } = await BlogPost.findOne({ where: { id } });
  if (userId !== tokenId) {
    return 'Unauthorized user';
  }
  return { type: null };
};

module.exports = {
  validateUpdateUser,
};